{
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.19.0",
    "npm": ">=11.17.0"
  },
  "packageManager": "npm@11.17.0",
  "scripts": {
    "build": "node site.mjs",
    "check": "node --check site.mjs && node --check test/site.test.mjs && npm run build && npm test",
    "test": "node --test"
  }
}
